# Rust
/target/
**/*.rs.bk
*.pdb
Cargo.lock

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Environment
.env
.env.local
*.key
*.pem

# Logs
*.log
logs/

# Config
config/local.toml

# Runtime state (SQLite DBs, outcome logs, scheduler lock)
outcomes/
*.sqlite
.claude/scheduled_tasks.lock

# Node — dashboard is pnpm-only; the pnpm-lock.yaml IS committed but
# never want stray npm/yarn lockfiles next to it.
node_modules/
dashboard/package-lock.json
dashboard/yarn.lock

# Sibling-aggregated repos that live INSIDE this tree for development
# convenience but carry their own .git and ship as standalone crates.
# Never commit upward. These contain internal strategy not for public eyes.
/trade/
/helm/
