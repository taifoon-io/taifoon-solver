# DELETED -- partner sidecar crate removed pre-hackathon.
# The Cargo.toml is kept only so any stragglers that may still resolve
# this directory have a parseable manifest; it is NOT a workspace member.
[package]
name = "partner-sidecar-removed"
version = "0.0.0"
edition = "2021"
publish = false
